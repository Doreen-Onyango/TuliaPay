{
  "name": "tuliapay-monorepo",
  "version": "1.0.0",
  "description": "TuliaPay Monorepo codebase",
  "private": true,
  "workspaces": [
    "smart-contracts",
    "backend",
    "frontend"
  ],
  "scripts": {
    "test:contracts": "npm run test --workspace=smart-contracts",
    "build:contracts": "npm run build --workspace=smart-contracts"
  }
}
